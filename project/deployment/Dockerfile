FROM python:3.12-slim AS backend

WORKDIR /app
COPY project/poc/backend/requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r /app/requirements.txt

COPY project/poc/backend/app /app/app
COPY project/poc/fixtures /app/fixtures

ENV POC_DB_PATH=/data/poc.sqlite
EXPOSE 8000

HEALTHCHECK --interval=30s --timeout=3s --retries=3 CMD python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=2)"

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
